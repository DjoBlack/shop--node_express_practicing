<%- include('../includes/head.ejs') %>
</head>
<body>

<%- include('../includes/nav.ejs') %>
    <main>
        <% if(products.length > 0) {%>
            <ul>
                <% products.forEach(cartItem => { %>
                    
                        <li>
                            <p><%= cartItem.prodData.title %> (x <%= cartItem.qty %>)</p>
                            <form action="/cart-remove-item" method="POST">
                                <button class="btn" type="submit">Remove Item</button>
                                <input type="hidden" value="<%= cartItem.prodData.id %>" name="productId">
                            </form>
                        </li>                  
                <% }) %>
            </ul>
            <h3>Total Price: $ <%= totalPrice %></h3>
        <% } else { %>
            <h2>No products in the cart yet!</h2>
        <% } %>
    </main>
<%- include('../includes/bottom.ejs') %>